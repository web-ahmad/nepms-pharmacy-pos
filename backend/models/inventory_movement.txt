from sqlalchemy import Column, String, Boolean, Float, Integer, ForeignKey, Text, Date
from sqlalchemy.orm import relationship
from .base import BaseModel

# ... (I will use replace_file_content instead because I don't want to overwrite the whole file, wait, write_to_file has Overwrite=False which means it fails if file exists. I will use replace_file_content to append).
