'use client';
import { CreditCard } from 'lucide-react';
import { PlaceholderPage } from '../PlaceholderPage';

export default function PlansPage() {
  return (
    <PlaceholderPage
      icon={CreditCard}
      title="Plans"
      description="Create and manage subscription plans, pricing tiers, and feature sets for pharmacies on the NEPMS platform."
      accent="#6366f1"
    />
  );
}
