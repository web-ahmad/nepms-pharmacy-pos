'use client';
import { Tag } from 'lucide-react';
import { PlaceholderPage } from '../PlaceholderPage';

export default function CouponsPage() {
  return (
    <PlaceholderPage
      icon={Tag}
      title="Coupons"
      description="Generate, manage and track promotional coupons and discount codes for pharmacy subscriptions."
      accent="#f59e0b"
    />
  );
}
